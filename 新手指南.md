# 新手指南（不需要懂技术）

这份说明假设你完全不会编程、没用过「终端」。按顺序做就行。

---

## 第一部分：你的 Mac

### 1. 下载

打开这个地址：https://github.com/taotao-river/wechat-auto-reply

点绿色的 **「Code」** 按钮 → **「Download ZIP」**。

下载完在「下载」文件夹里找到那个 zip，**双击解压**。会得到一个文件夹。

### 2. 双击安装

打开那个文件夹，找到 **「安装到Mac.command」**。

> ⚠️ **Mac 一定会拦这个文件**，因为它是从网上下载的。这是正常的，按下面做。
>
> **先试**：右键点它 → 选「打开」→ 弹窗里点「打开」。
>
> **如果弹出的窗口只有「完成」和「移到废纸篓」两个按钮**（新版 macOS 是这样），
> 那就是右键这招没用了。**点「完成」，千万别点「移到废纸篓」**，然后：
>
> 1. 左上角  → **系统设置**
> 2. 左侧选 **隐私与安全性**
> 3. 右侧**滚到最底下**，找到 **安全性** 这一块
> 4. 会看到一行「已阻止使用"安装到Mac.command"…」，旁边有 **「仍要打开」**
> 5. 点它 → 输密码或按指纹 → 再弹的窗里点「打开」
>
> 那个「仍要打开」按钮只在刚被拦截后的几分钟内出现。找不到的话，
> 回去再双击一次那个文件让它再拦一次，然后马上去系统设置就有了。
>
> 弄过这一次之后，以后直接双击就行，不会再拦。

会弹出一个黑色窗口，按提示一路按键就行。中途可能要你输 Mac 的开机密码。

> ⚠️ **如果跳出「需要安装命令行开发者工具」这个弹窗：点「以后」或「取消」，
> 不要点安装。**
>
> 那是 Xcode 的开发者工具，又大又容易下载失败（国内尤其），
> 而这个程序根本不需要它。
>
> 点了「以后」之后，脚本会告诉你去 Python 官网下载一个安装包
> （找 **macOS 64-bit universal2 installer**，双击一路继续即可），
> 装完重新双击「安装到Mac.command」就能继续。
>
> **万一弹窗关了还一直弹**：把那个黑色终端窗口整个关掉就停了。
> 还在弹的话，打开「活动监视器」，找到
> `Install Command Line Developer Tools`，选中后点左上角 ✕ 强制退出。

装完它会自动打开一个设置页面，让你**给「终端」打开辅助功能开关**。
这一步不做的话，程序读不到微信消息。

> 如果那个列表里找不到「终端」：点列表下面的 **➕**，
> 然后从「应用程序 → 实用工具」里选「终端」。

### 3. 装到一半会问你十个问题

装到一半它会停下来问你十个问题，像这样：

```
  ── 第 3 题 / 共 10 题 ──

  你平时打字是什么感觉？
  （这一题最影响像不像你本人）
    1. 简短随便，跟熟人聊天那种
    2. 客气一点，会用「您」
    3. 热情，愿意多聊两句，爱开玩笑
    4. 能少说就少说，一两个字就完事
  > 1
```

**打数字按回车就行**，八道是选择题，一分钟能答完。两道填空题
（「别人发『在吗』你自己会怎么回」和「有什么绝对不能替你答应」）
直接回车就能跳过。

答完它会先把生成的话摆出来给你看：

```
  别人说「在吗」        → 在，怎么了
  别人约你吃饭          → 我看下日程，晚点回你
  别人问事情办得怎样    → 在弄了，这两天给你结果
  别人推销、拉群        → 这个我不太需要，谢谢
  其他都没匹配上        → 我白天要上班，手机不太方便看，看到会尽快回你
```

不满意就输 `n` 重答一遍。满意直接回车。

> 为什么要问这些：这套系统最难写的就是「你是个什么样的人」。
> 与其让你对着一个空框发愁，不如问几个你张口就能答的问题，
> 它自己把话组织好。
>
> **这几句话是你的联系人会真正看到的**，不满意就多重答几遍，
> 值得花两分钟。这一步不会发任何消息。

### 4. 装完后，文件夹里会多出三个文件

安装完，回到那个文件夹，你会看到多了三个带数字的文件：

```
1 检查微信.command
2 试运行（不真发消息）.command
3 开始自动回复.command
```

**按数字顺序，一个一个双击就行。** 下面分别说。

### 5. 双击「1 检查微信」

**先确认微信已经登录、主窗口是打开着的**，然后双击这个文件。

这一步**只看不动**：不读消息，也不发消息，纯粹检查程序能不能认出
微信的界面。会弹出一个黑窗口，显示类似这样：

```
  [OK] splitter group 1
  [OK] table 1（会话列表）
  [OK] 会话列表有 23 行

=== 前 3 个会话读出来长什么样 ===
  会话 1: 小王
  会话 2: 项目组(8)
```

- **全是 `[OK]`，而且会话名是你真实的联系人** → 成功了，继续下一步
- **出现 `[X ]`** → 把这个窗口**整个截图**发给帮你配置的人

> 为什么要有这一步：这个程序是靠「读微信的界面」来找消息的，
> 而微信每次更新界面结构都可能变。先检查一下，比直接跑起来
> 看到一句看不懂的报错有用得多。

### 6. 双击「2 试运行（不真发消息）」

**这一步不会发任何消息出去**，只显示"本来会回什么"。

你要在窗口里找到类似这样的内容：

```
「小王」最后一条: 在吗
  [DRY-RUN] 本应回复: 在，怎么了（自动回复）
```

- **会话名和消息内容都对** → 成功了，继续下一步
- **一直没反应** → 让朋友给你发条消息试试（它只处理**未读**的会话）

> ⚠️ 有一点要知道：虽然不发消息，但程序读消息时需要点开会话，
> 所以**未读会被标成已读**。这是这种方案绕不开的。

关掉窗口就停了。

**如果窗口里显示「未授予辅助功能权限」** → 回第 2 步最后那个授权没做好。
勾完「终端」之后，要把终端**完全退出（⌘Q）再重新双击**才生效。

### 7. 双击「3 开始自动回复」

确认前两步都没问题后，双击这个。**从这一刻起它会真的发消息。**

这个窗口**得一直开着**，关掉就停了。可以最小化。

**建议再做一件事**：系统设置 → 电池 → 把「睡眠」设成「永不」。
否则合上盖子自动回复就停了。

> 第一次用建议先只观察半天，别急着离开电脑。

### 8. 改回复内容

**最省事的办法：重新双击一次「安装到Mac.command」**，
它会再问你一遍那十道题，整套话重新生成。
（原来的配置会自动备份，改坏了也丢不了。）

想只改某一句的话，用「文本编辑」打开文件夹里的 `core` → `config.yaml`。
里面中文注释写得很清楚，你主要改这几处：

```yaml
rules:
  - name: 打招呼
    match:
      type: keyword
      any: ["在吗", "在么", "忙吗"]      # 收到这些词时
    reply:
      - "在的，我这会儿在忙，稍后回你"    # 就回这句
      - "在，手上有点事，等下详细说"      # 或这句（轮流用）
```

改完保存，然后**关掉「3 开始自动回复」窗口再双击打开一次**就生效了。

> 改的时候注意：不要动每行前面的空格数量，这个格式对空格很敏感。

---

## 第二部分：你朋友的安卓

### 1. 把这个链接发给他

**你什么都不用下载。** 直接把下面这行发给你朋友：

```
https://github.com/taotao-river/wechat-auto-reply/releases/latest/download/wechat-auto-reply.apk
```

就这样。这是个固定地址，永远指向最新版本，不用登录任何账号。

> 微信里链接可能点不开（微信会拦外部下载）。让他**长按复制**，
> 粘到手机自带的浏览器里打开。或者你用别的方式发给他，比如短信、QQ。

### 2. 他手机上安装

他用**手机浏览器**打开那个链接，会直接开始下载。下载完点开安装。

中途会提示「**禁止安装未知应用**」——按提示允许一下就行
（不同手机说法不一样，大意都是"允许浏览器安装应用"）。

> 这是绕不开的：这个 App 不在应用商店里，只能这样装。
> 所有安卓手机对商店外的应用都会拦一下。

### 3. 他第一次打开会先答十道题

装完打开，App 会直接进一个问答页，一屏一题：

> **你平时打字是什么感觉？**
> ○ 简短随便，跟熟人聊天那种
> ○ 客气一点，会用「您」
> ○ 热情，愿意多聊两句，爱开玩笑
> ○ 能少说就少说，一两个字就完事

**点一下就跳下一题**，十道题里八道是这样的单选，全程基本不用打字。
两道填空可以点「这题跳过」。

答完会先把生成的话摆出来给他看，点「就用这套」才会存下来。
不满意可以点「重新答一遍」。

**让他自己答，别替他答**——这一步决定了回复像不像他本人。
以后想改，设置页里有个「回答几个问题，自动生成」可以随时重来。

### 4. 选「怎么回」：关键词，还是 AI

App 里有一栏 **「怎么回」**，两个选项：

**① 按关键词回固定的话**（默认，装完就能用）

事先写好「收到『在吗』就回『在的，稍后回你』」。不联网、不花钱、
不需要注册任何东西。缺点也很明显：只能回你想到的那几句，
别人说点别的就只能回兜底那一句——这跟当年 QQ 的自动回复是一个东西。

**② 让 AI 按你的说话方式现写**

每条消息现写一句，看得懂对方在说什么。你不用去猜别人会发什么，
只要在下面填清楚**你是个什么样的人、各种情况怎么应对**，AI 会照着判断。

选了 AI 之后，下面还要二选一：

- **「我自己注册一个」（推荐）** —— **不知道选哪个就选豆包**，
  聊天的中文语气这几家里它最自然。去
  [火山方舟控制台](https://console.volcengine.com/ark) 注册，
  在「API Key」页面点新建，把那串字符粘进 App。
  国内直接能用，不用翻墙；新注册送额度，自动回复用量很小基本花不到钱。
  App 里点一下「豆包」按钮，接口地址和模型名会自动填好，他只需要粘 key。
  这条路他**谁也不依赖**，你那边关不关机都不影响他。

  > 豆包有个小坑：如果「试一试」提示**「模型名不对」**，
  > 去方舟控制台把模型 ID（或者「推理接入点」里 `ep-` 开头那串）
  > 复制到 App 的「模型名」那一栏。这一家的模型 ID 会随版本变，
  > 预填的那个可能过期。
  >
  > 其他可选：DeepSeek、通义千问、智谱 GLM、Moonshot，点对应按钮即可。

- **「用别人给我的地址」** —— 你在自己电脑上跑起本仓库的
  Python 服务（见 `docs/deployment.md`），把地址和口令发给他，
  他粘进 App 就行，不用注册任何账号。
  代价是：他的消息会经过你的服务器，回复也由你那边生成，
  而且你电脑关机他就不回了。**只推荐在你俩都清楚这一点的前提下用。**

> 两个选项随时可以换，换完点「保存设置」就生效。

### 5. 让他先「试一试」

App 里最下面有个 **「试一试（不会真的发出去）」**：输入一句话，
点「看看会回什么」，就能看到会回什么、或者为什么不回。

**让他在打开总开关之前先试几句**，把语气调成他自己的。
改完点「保存设置」。试的时候不会发消息，也不占用每日额度。

选了 AI 模式的话，这里会**真的调一次接口**，所以顺便也验证了
key 有没有填对——如果 key 是错的，这里就会告诉他。

### 6. 授权 + 打开开关

App 里点 **「授予通知使用权（必须）」**，在跳转的系统页面里找到
「微信自动回复」并打开。不给这个权限，程序看不到微信消息。

回到 App，把最上面那个 **大开关拨到开**。上面会显示「✅ 正在工作中」。

**就这样，完事了。**

用关键词模式的话，到此为止不需要电脑、不需要连你的 Wi-Fi、不需要任何服务器。
用 AI 模式的话，手机需要能上网，但仍然不需要电脑（除非他选的是「用别人给我的地址」，
那就得你那边的服务开着）。

### 7. 教他怎么随时开关

自动回复不是一直要开着的。两个办法关掉：

- 打开 App 把大开关拨回去
- 或者：下拉通知栏 → 点编辑（铅笔图标）→ 把「微信自动回复」
  拖进快捷开关。以后下拉点一下就切换，跟开关手电筒一样

> 已经排队等着发的消息，在开关关掉后也不会发出去。

### 8. 让他注意省电限制

有些手机（小米、华为、OPPO 等）会自动杀后台。让他去设置里
把「微信自动回复」和「微信」都加进省电白名单，否则可能过一阵就不工作了。

---

## 常见问题

**Q：会不会被封号？**

有风险，**不能保证一定没事**。但可以把风险压得很低，下面说清楚。

先说腾讯明文禁止的是什么：**非官方客户端、外挂**（协议号、微信多开、
WeTool 这类）。这些会伪造客户端或直接跑微信的私有协议，属于明确违规。

**这个程序不是那种东西**：它不改微信、不碰微信的协议。Mac 上用的是
系统的辅助功能（等于有人在替你点鼠标、敲键盘），安卓上用的是系统
通知栏的官方回复接口。在微信看来，就是一个正常客户端上有人在打字。

但这不等于零风险。**真正最可能让你出事的，是被人举报** ——
不是什么算法识别。自动回复容易惹人不快：答非所问、该认真回的时候
敷衍了，对方一举报，你就进了复核队列。

所以按有效性排序，这样做：

| 做法 | 为什么 |
|---|---|
| **1. 只对三五个熟人开** | 最有效，胜过其他所有手段。熟人不会举报你 |
| **2. 保留「（自动回复）」后缀** | 对方一眼知道是机器，就没有「被骗」的感觉 |
| **3. 量小一点** | 一天几条，不是几百条 |
| **4. 别对陌生人、群、客户、领导开** | 出错代价最高的几类 |
| **5. 有小号先用小号跑一周** | 最稳 |
| **6. 一有人表示反感立刻关** | 别硬撑 |

第 1 条程序里直接支持：问答最后一题「先只对哪几个人开」，
填上名字之后，**只有名单里的人会收到自动回复，其他人一律不回**。
安卓设置页里也有同样的输入框。

程序自带的技术防护（都是默认开着的）：

- 不秒回：随机延迟，还会按回复长度加上「打字时间」
- 同一个人半小时内只回一次，每天最多 5 条
- **两条回复之间至少隔 45 秒** —— 真人不可能一秒切一个会话回一条
- 每小时最多 30 条、每天最多 100 条
- **发给不同人的话术自动轮换** —— 一百个人收到同一句话是最明显的机器特征
- 只在 09:00–23:00 回
- 群消息默认不回
- 转账、红包、验证码这类消息一律不碰

**Q：对方知道是机器人回的吗？**
默认每条回复末尾会带「（自动回复）」。建议保留，免得引起误会。

**Q：转账、红包会不会被自动回复？**
不会。这类消息程序直接跳过交给你本人处理，这个行为改不了。

**Q：怎么临时停掉？**
- Mac：关掉「3 开始自动回复」那个窗口
- 安卓：App 里把大开关拨回去，或者用下拉通知栏的快捷开关

**Q：重启后还要重新弄吗？**
- Mac：后台服务会自动启动，但「3 开始自动回复」要重新双击一次
- 安卓：不用管，开关状态会记住

**Q：安卓那边要不要电脑？**
不要。关键词模式下所有判断都在手机里完成，不联网、不上传消息、也不连你的电脑。
选了 AI 模式且用「自己注册的 key」，手机联网就行，还是不需要电脑。
只有选「用别人给我的地址」时，才依赖对方那台电脑开着。

**Q：AI 模式会把我的聊天记录传出去吗？**
会把**需要回复的那条消息**和最近几轮上下文发给你选的那家接口，
否则它没法知道该回什么。要注意的是：

- 含转账、红包、验证码、借钱这类字样的消息**根本不会发出去**，
  程序在本机就拦掉了，压根轮不到模型
- 黑名单里的人、按策略不回的群消息，也都在本机就拦掉了，不会发出去
- 上下文只存在手机内存里，最多留 8 轮、1 小时，App 一关就没了，不落盘

介意的话就用关键词模式，那个模式一个字节都不往外发。

**Q：AI 会不会乱答应事情？**
提示词里硬性写了不承诺金额时间地点、不答应转账借钱代付、不自称 AI。
但这些是**提示词**，是概率性的，不是保险。真正的保险是本机那几条：
敏感词、黑名单、群聊策略、频率限制——这几条在消息发给模型之前就执行完了，
模型只决定「说什么」，不决定「该不该说」。

所以还是那句话：一开始只对两三个熟人开启，观察几天。

**Q：AI 模式下它一直不回复，怎么查？**
用「试一试」输一句话，它会**真的调一次接口**，然后直接告诉你哪儿不对：

| 它说 | 怎么办 |
|---|---|
| API Key 不对或者没权限 | key 没粘全，或者复制到了别的东西 |
| 模型名不对，去控制台把模型 ID 复制过来 | 常见于豆包，见上面那个小坑 |
| 余额不足 | 去控制台充点钱 |
| 调用太频繁，或者免费额度用完了 | 等一会儿，或者换一家 |
| 连不上接口 | 手机网络问题 |
| 连不上对方的地址，可能他电脑没开机 | 用的是「别人给的地址」这条路 |

**Q：AI 模式要花钱吗？**
看你选的那家怎么收费。自动回复的用量很小（一条消息几百个字符），
多数家新注册送的额度够用很久。
另外程序本身有限流：同一个人半小时内只回一次、每天最多几条，
不会出现狂刷接口的情况。接口调用失败时程序直接不回，也不会重试烧钱。

**Q：我改坏了配置文件怎么办？**
重新双击一次「安装到Mac.command」，它会再问一遍那十道题并生成一份完整的。
旧的会自动备份成 `core/config.yaml.bak`，丢不了。

**Q：问答生成的话我不太满意，是不是就只能用这几句？**
不是。那几句是**示范**：

- 开了 AI 模式的话，AI 是照着这个语气自己判断该说什么的，
  不是只会回这几句——别人说什么它都能接
- 用关键词模式的话，确实只有那几句，但你可以在配置文件
  （或安卓的设置页）里随便加

另外第 9 题「别人发『在吗』你自己会怎么回」建议一定要自己填，
用你平时真会说的话。你自己的一句话，比它按风格挑的任何一句都准。

---

## 需要帮忙的时候

出问题时，把这些发给帮你配置的人：

1. 出错窗口的**完整截图**
2. 如果是 Mac：文件夹里 `var/server.err.log` 这个文件
3. 你的微信版本号（微信 → 设置 → 关于微信）

微信版本号很重要——这个程序读微信界面的方式跟版本有关，
换个版本可能就要改一下。
